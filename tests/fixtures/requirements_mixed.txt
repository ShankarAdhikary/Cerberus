# sample requirements file with mixed line types
requests==2.28.0
lodash-clone==1.0.0

# unpinned entries should be skipped, not silently dropped
flask>=2.0
django

# VCS/URL requirements should also be skipped
git+https://github.com/example/example.git@main#egg=example
-e ./local-package

numpy==1.26.4
