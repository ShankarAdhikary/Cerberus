# Tracker.md — Task Tracker

Aligned 1:1 with `ImplementationPlan.md`. Check off items only when the
corresponding phase's "Exit criteria" are fully met, including tests —
not when the code merely runs once locally.

## Done

- [x] Scaffold `dep_gate/` package (`__init__.py`)
- [x] Implement `parse_npm_lockfile()` (v1/v2/v3, transitive deps)
- [x] Implement `parse_requirements_txt()` (pinned `==` lines, warn on skip)
- [x] Implement `parse_lockfile()` dispatch function
- [x] Implement `_dedupe()` by `(ecosystem, name, version)`
- [x] Implement `batch_query()` against `POST /v1/querybatch`
- [x] Implement `hydrate_vulns()` against `GET /v1/vulns/{id}`
- [x] Implement `_request_with_retries()` shared backoff wrapper
- [x] Implement `severity.assess()` (CVSS vector → `database_specific` → `UNKNOWN` precedence)
- [x] Implement `severity.fixed_version()` (package-scoped fix lookup)
- [x] Implement `argparse` CLI with `--file`, `--fail-on`, `--json`, `--fail-open`
- [x] Implement `run()` orchestration with correct exit codes (0/1/2)
- [x] Implement Rich findings table with optional plain-text fallback
- [x] Implement grep-able `SCAN FAILED` / `SCAN PASSED` summary line
- [x] Implement `diff.get_base_dependencies()` via `git show` subprocess
- [x] Implement `diff.diff_dependencies()` set-diff logic
- [x] Wire `--diff-only` / `--base-ref` into `cli.run()`
- [x] Write `.github/workflows/security-scan.yml` with `fetch-depth: 0`
- [x] End-to-end manual verification: mocked-OSV CLI run, real temp-git-repo diff test
- [x] Formalize `unittest`/`pytest` test suite under `tests/` (59 tests: lockfile.py, severity.py, osv_client.py mocked-`requests.Session`, diff.py real temp-dir git repo, cli.py end-to-end with only the osv_client boundary mocked — see verification pass recorded below)
- [x] Fix `diff.get_base_dependencies()` treating every non-zero `git show` exit as "file absent at ref" — now raises `GitDiffError` for an unresolvable ref / non-repo per TechSpec.md §4, only returns `[]` for a genuinely-absent path at a resolvable ref (regression tests in `tests/test_diff.py`)
- [x] Fix `severity.fixed_version()` picking the lexicographically-lowest fix string (e.g. "4.17.19" < "4.17.2") instead of the numerically-lowest version — added `_version_sort_key()` natural-sort helper (regression tests in `tests/test_severity.py`)
- [x] Add `Go` ecosystem support: `parse_go_sum()` (`go.sum` → `ecosystem="Go"` deps, content-hash/`/go.mod`-hash rows collapsed via dedupe), wired into `parse_lockfile()` dispatch, `--file` help text, and `.github/workflows/security-scan.yml`'s go.sum scan step; docs updated (PRD.md moved it V2→V1, TechSpec.md §2.1, README.md)
- [x] Add `Rust` ecosystem support: `parse_cargo_lock()` (`Cargo.lock` TOML via stdlib `tomllib` → `ecosystem="crates.io"` deps, registry-only — path/git deps skipped), wired into `parse_lockfile()` dispatch, `--file` help text, and `.github/workflows/security-scan.yml`'s Cargo.lock scan step; docs updated (PRD.md moved it V2→V1, TechSpec.md §2.1, README.md); no new third-party dependency (`tomllib` is stdlib on the project's Python 3.11+ baseline)
- [x] Fix `severity.fixed_version()` recommending a downgrade when one OSV record covers multiple disjoint vulnerable ranges for the same package (confirmed live via GHSA-43w2-9j62-hq99/RUSTSEC-2021-0003 — smallvec@crates.io) — added optional `installed_version` param so only the range actually containing the installed version is used; wired through from `cli.run()`; docs updated (Schema.md §5.3, TechSpec.md §2.3); regression tests in `tests/test_severity.py` and `tests/test_cli.py`
- [x] Add `--verbose` flag surfacing `Severity.source` — purely additive: extra "Source" table column (Rich + plain-text) and extra `"source"` JSON field, both omitted by default; does not change severity, fix recommendation, or exit code. Docs updated (AppFlow.md flags table, Design.md §3.2, Schema.md `Vulnerability`/`Severity`, README.md); tests in `tests/test_cli.py`
- [x] Add SBOM export: new `sbom.py` module (`build_sbom()`, pure/I-O free, ecosystem→purl-type mapping), `--sbom PATH` flag writes a CycloneDX 1.5 JSON document from whichever dependency list was scanned (respects `--diff-only`), independent of vulnerability findings — written even on a clean scan or OSV.dev outage, not written on a usage error. Not wired into the PR-gate CI workflow (would only ever produce a partial/delta SBOM there — documented in README.md instead). Docs updated (TechSpec.md §1/§2.5, AppFlow.md flags table + execution tree, PRD.md, README.md); tests in `tests/test_sbom.py` and `tests/test_cli.py`
- [x] Add `.dep-gate-ignore.yml` suppression-file support: new `suppress.py` module (`load_suppressions()`/`find_active_suppression()`, hand-rolled minimal parser for a deliberately narrow YAML-list-of-mappings subset — no new `PyYAML` dependency), `--ignore-file PATH` flag; required `vuln_id`/`expires`/`reason` per entry (missing/blank → exit 2), optional `package` scoping, expiry enforced automatically (string-comparable ISO dates). Suppressed findings stay visible (`SEVERITY (suppressed)`) and are excluded only from the `--fail-on` `blocking` check, never hidden — live-verified against real OSV.dev data. Docs updated (PRD.md moved it V2→V1, AppFlow.md flags table/degradation table/execution tree, Design.md, Schema.md §3.5, TechSpec.md §1/§2.6, README.md); tests in `tests/test_suppress.py` and `tests/test_cli.py`
- [x] Add local hydration cache: new `cache.py` module (`load_cache()`/`save_cache()`/`get_fresh()`/`put()`, flat JSON keyed by vuln_id, `CACHE_TTL_SECONDS=6h`, corrupt/missing cache file treated as empty rather than crashing), `--cache-file PATH` flag; deliberately TTL-based rather than using OSV's per-result `modified` timestamp, to avoid changing `batch_query()`'s tested return shape (reasoning recorded in TechSpec.md §2.8, superseding Schema.md §5.1's earlier speculative note). Without `--cache-file`, behavior is byte-for-byte unchanged from before. Wired into `.github/workflows/security-scan.yml` via `actions/cache` (restore-keys prefix pattern) — ~~not exercised against a live GitHub Actions run~~ **since live-verified for real** (see "Live-verify SARIF-upload and OSV-cache CI wiring" below — this uncovered and fixed a genuine bug: the cache never persisted on a failing scan). Live-verified locally too: cold run populated 6 cache entries from real OSV.dev data; warm run reused them (same findings, same exit code, ~3.4x faster); CLI-level tests assert zero `hydrate_vulns()` calls on a full cache hit and correct re-fetch of expired entries. Docs updated (PRD.md moved it to V1, AppFlow.md flags table + execution tree, Schema.md §5.1, TechSpec.md §1/§2.8, README.md); tests in `tests/test_cache.py` and `tests/test_cli.py`
- [x] Add SARIF output: new `sarif.py` module (`build_sarif()`, pure/I-O free, rule dedup by vuln_id, severity→SARIF-level mapping CRITICAL/HIGH→error, MODERATE→warning, LOW/UNKNOWN→note, whole-file locations, suppressed findings marked via SARIF's native `result.suppressions` rather than omitted), `--sarif PATH` flag alongside (not instead of) `--json`, not written on a usage error. Wired into `.github/workflows/security-scan.yml` (added `security-events: write` permission + `github/codeql-action/upload-sarif@v3` step, `continue-on-error: true` so an upload failure can't mask a real scan failure) — ~~this workflow wiring has not been exercised against a live GitHub Actions run~~ **since live-verified for real** (see "Live-verify SARIF-upload and OSV-cache CI wiring" below: pulled the ingested SARIF back from GitHub's own API and confirmed byte-correct round-trip of all rules/results/severity levels). Docs updated (PRD.md moved it to V1, AppFlow.md flags table + execution tree, TechSpec.md §1/§2.7); tests in `tests/test_sarif.py` and `tests/test_cli.py`
- [x] Add GitHub PR auto-commenting: new `github_client.py` module (the tool's second and only other network egress point besides `osv_client.py`) — `render_pr_comment()` (pure, renders Design.md §4's exact structure, blocking-findings-only, never a raw CVSS vector), `find_existing_comment_id()`/`upsert_comment()` (mocked-`requests.Session`-tested, same pattern as `osv_client.py`) using a hidden `COMMENT_MARKER` for idempotent per-PR updates. `--pr-comment`/`--github-repo`/`--github-pr-number` flags; token read from a `GITHUB_TOKEN` env var only (never a CLI argument, to keep it out of process listings/CI logs); required-args validation happens first, right after argument parsing, before any OSV.dev call (fail fast on a usage error, exit 2); a comment-posting failure prints a warning but never changes the scan's own exit code. **Auth model resolved** (was Blocked): `GITHUB_TOKEN` with job-scoped `permissions: pull-requests: write`, not a GitHub App — this is a single-repo PR-gate tool, and a GitHub App would add registration/installation/secret-management infrastructure that PRD.md's own NFRs reject ("doesn't require standing up new infrastructure" — DevOps persona); `GITHUB_TOKEN` is the standard zero-setup pattern for this exact idempotent-bot-comment use case. **Deliberately not wired into `.github/workflows/security-scan.yml`**: that workflow scans up to four lockfiles in separate steps, and each `--pr-comment` invocation would find-and-overwrite the same idempotent comment, leaving it reflecting only the last ecosystem scanned — worse than no comment. A correct combined comment needs one invocation across all changed lockfiles, out of scope for the current one-file-per-invocation `cli.py` design; documented (not silently dropped) in README.md with a single-lockfile usage example instead. ~~Not exercised against the real GitHub API~~ — **since verified live** (see the later "Live-verify SARIF-upload and OSV-cache CI wiring" entry below): `find_existing_comment_id()`/`upsert_comment()` were run for real against `ShankarAdhikary/Cerberus#1`, confirming both comment creation and idempotent update. Docs updated (PRD.md moved it to V1 + zero-telemetry NFR, TechSpec.md §1/§2.9, AppFlow.md flags table/execution tree/degradation table/§4, Design.md §4, README.md); tests in `tests/test_github_client.py` and `tests/test_cli.py`
- [x] Fix `pytest.ini` missing `pythonpath = .` — bare `pytest -v` (the conventional invocation) failed *all* test collection with `ModuleNotFoundError: No module named 'dep_gate'` on every one of the 10 test files, since the project has no `pyproject.toml`/`setup.py` (so `dep_gate` was never installed, not even editable) and `pytest.ini` only set `testpaths`, never adding the repo root to `sys.path`. Every verification run in this session used `python -m pytest` instead, which happens to add the CWD to `sys.path` itself — that's why this went unnoticed: the working invocation masked the broken one. Confirmed reproduced, then fixed with one line; both `pytest -v` and `python -m pytest -v` now pass identically (138/138). Also note: `.github/workflows/security-scan.yml` never runs the test suite at all (it only invokes `dep_gate.cli` directly to scan), so this breakage had zero effect on CI and would only ever surface for a human running the standard command locally — worth considering a dedicated test-suite CI job if that matters going forward.
- [x] Add `LICENSE` (MIT) — the repo had none, a real legal ambiguity once other engineers or the public can see it. Referenced from README.md.
- [x] Re-confirm live OSV.dev integration hasn't drifted — a fresh, explicit check right now (`lodash@4.17.15`, `--verbose`) reproduced the same real findings/severities/fix-versions/exit-code as every other live check this session; no schema drift found. (This project's severity-parsing path was in fact live-verified extensively throughout this session — across npm/PyPI/Go/crates.io and a real GitHub Actions run — contrary to review feedback suggesting it had only been checked against mocked responses; this entry exists to make that verification history explicit and re-confirmed, in one place.)
- [x] Add retry/backoff to `github_client.py` — `find_existing_comment_id()`/`upsert_comment()` previously called `raise_for_status()` with no retry at all, so a single transient GitHub API hiccup or secondary rate limit immediately produced a (non-blocking, but noisy) "failed to post PR comment" warning; this would happen often enough to be genuinely noisy for anyone running `--pr-comment` across many repos/PRs on a low-quota token. Added `_request_with_retries()`, same shape as `osv_client.py`'s function of the same name (`MAX_RETRIES=3`, exponential backoff, retries on 429/5xx/connection errors/timeouts), additionally honoring GitHub's `Retry-After` header (sent on secondary rate limits) when present. Existing tests updated to assert against `session.request` (not `session.get`/`.post`/`.patch` directly, matching how the retry wrapper actually calls it); 5 new tests added (retry-then-succeed on 429, `Retry-After` respected, retry on connection error, exhaustion raises, timeout always set). Docs updated (TechSpec.md §2.9); tests in `tests/test_github_client.py`
- [x] **Live-verify SARIF-upload and OSV-cache CI wiring for real** (previously untested against actual GitHub Actions) — required making the repo public (was private; code scanning needs GitHub Advanced Security, confirmed via API error `"Advanced security has not been purchased"`, free on public repos, not available here on private without a paid plan). Ran 5 real workflow runs on PR #1 (`ShankarAdhikary/Cerberus#1`) to get there:
  - **SARIF ingestion: fully proven.** Pulled the SARIF content back from GitHub's own `code-scanning/analyses/{id}` endpoint (with `Accept: application/sarif+json`) and confirmed all 8 rules and all 8 results round-tripped byte-correctly, including the CRITICAL/HIGH→`error`, MODERATE→`warning` level mapping — `github/codeql-action/upload-sarif@v3` genuinely ingests `sarif.py`'s output correctly. (The repo-level `/code-scanning/alerts` endpoint stays empty for PR-branch analyses — that's expected GitHub behavior, persistent alerts only populate from the default branch; PR analyses surface via PR checks/annotations instead. Not a bug.)
  - **Found and fixed a real bug: the OSV cache never persisted on a failing scan.** `actions/cache@v4`'s default `save-always: false` only saves the cache when the *job succeeds* — but this gate's job legitimately fails on real findings, which is exactly the case (a PR sitting on an unfixed vulnerability across repeated pushes) that most needs a persisted cache. Run 1 confirmed this: the "Restore OSV hydration cache" step's POST (save) step never even appeared in the log. First fix attempt (`save-always: true`) looked right but is a **deprecated, non-functional** input — GitHub's own deprecation warning says so, and runs 2–3 confirmed it live ("Cache not found" persisted regardless). Correct fix (run 4 confirmed): split into separate `actions/cache/restore@v4` + `actions/cache/save@v4` steps, with the save step carrying `if: always()`. Run 4's log: `Cache saved with key: dep-gate-osv-cache-34934767002`. **Run 5 then confirmed genuine cross-run persistence**: `Cache hit for restore-key: dep-gate-osv-cache-34934767002` — a *different* run's restore step found and reused the *previous* run's saved cache via the `restore-keys` prefix match. Docs updated (TechSpec.md §2.8).
  - **`--pr-comment` against the real GitHub API: fully proven, including idempotency** — ran `python -m dep_gate.cli --pr-comment --github-repo ShankarAdhikary/Cerberus --github-pr-number 1` locally twice with a real `GITHUB_TOKEN` (from `gh auth token`). First call created comment `id: 5675397384` on the real PR with the correct rendered body (verified against Design.md §4's structure). Second call confirmed idempotent update, not a duplicate: same comment `id`, unchanged `created_at`, `updated_at` moved forward — genuine `PATCH`, not create. This closes the "mocked-only" gap noted in the earlier Done entry for this feature. The documented multi-lockfile comment-overwrite limitation (still not wired into the shipped workflow) stands as previously recorded — that's a real design gap, not a testing gap, and remains unaddressed.
- [x] **Found and fixed a second real gap while chasing the above: PR-branch code-scanning results never surface as visible alerts.** GitHub's code-scanning check only ever reports a PR's findings as "new" relative to the last analysis on the *default branch* — confirmed live: a real PR's `dep-gate` check reported "No new alerts in code changed by this pull request" despite its own SARIF genuinely containing 8 findings (verified via the raw analysis JSON), because `main` had never been scanned (it didn't even have a lockfile). Fixed by adding a `push: branches: [main]` trigger that runs a *full* (non-diff) scan to establish that baseline — `--diff-only` is now conditional on `github.event_name == 'pull_request'` via a GitHub Actions expression (`github.base_ref` doesn't exist on `push` events, so the flag can't be unconditional). Docs updated (PRD.md §3 "Full audit" use case, TechSpec.md §4, README.md CI integration section).
- [x] **Production-readiness cleanup**: version bumped `0.1.0` → `1.0.0` (`dep_gate/__init__.py`) to match the now-complete V1 feature set; `requirements.txt`/`requirements-dev.txt` switched from version ranges to exact pins (`requests==2.32.5`, `cvss==3.6`, `rich==15.0.0`, `pytest==9.1.1` — each confirmed to actually exist on PyPI, not just locally installed) — a security tool's own supply chain deserves the same exactness it demands of what it scans; added `LICENSE` (MIT). Ran the `ruff`/`black` lint pass Rules.md requires but that had never actually been run in this project until now (both tools installed via `pipx`, not added as project dependencies) — 95 findings: ~86 were mechanical/safe (typing modernization `Dict`→`dict`/`Optional`→`X | None` matching `from __future__ import annotations`'s own stated purpose per Rules.md, unused-import cleanup, import sorting, a `startswith` merge, a nested-`if`/`with` collapse) and applied via autofix or by hand; 2 `BLE001` "blind except" findings were **not** weakened — both are intentional, already-documented safety nets (a comment-posting failure must never change the exit code; a malformed CVSS vector must degrade, not raise) and got an explicit `# noqa: BLE001` instead; 1 `DTZ011` (`datetime.date.today()` in `suppress.py`) was a genuine correctness fix, not just style — a suppression's expiry is a fixed calendar date and must evaluate identically regardless of the machine's local timezone, so the default now explicitly uses UTC. Full suite (142/142) and a live OSV.dev check re-run clean after every step of this pass.
- [x] **Verified the `push`-to-`main` baseline fix for real, then cleaned up the demo artifacts.** The push carrying the version bump/pinning/lint/demo-lockfile commits triggered a real `push`-event workflow run on `main` (id `34948542686`) — confirmed via its own log that it ran the *full* scan (no `--diff-only` in the actual command line, exactly as the conditional expression should produce for a non-`pull_request` event) and correctly failed (exit 1) on the same 4 blocking findings. Checked `/code-scanning/alerts` immediately after: **all 8 alerts are now genuinely `"state": "open"`** with correct severities/messages/`html_url`s pointing at `.../security/code-scanning/1` through `/8` — the exact gap this whole verification arc started from is now closed with definitive evidence, not just a claim. Closed PR #1 (commented explaining why, then closed without merging - it was a testbed, never meant to land) and deleted `demo/add-vulnerable-dep` both locally and on the remote, now that the demo lockfile lives permanently on `main` as a single clean commit.

## To Do

- [ ] *(none currently — all PRD.md V1 items implemented and tested)*

## In Progress

- [ ] *(none currently — update this section when work starts)*

## Blocked

- [ ] *(none currently)*

## Notes for Agents Updating This Tracker

- Move an item from **To Do** → **In Progress** the moment you start
  editing files for it, not after you finish — this file should reflect
  real-time state, not be reconstructed after the fact.
- Never mark an item **Done** without a corresponding test existing in
  the repo for it (see `Rules.md`).
- If an item turns out to need a decision or external input before it
  can proceed, move it to **Blocked** with a one-line reason, don't leave
  it silently stuck in **In Progress**.
